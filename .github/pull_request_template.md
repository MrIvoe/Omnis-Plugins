## What does this PR do, and why?

<!-- The "why" matters more than the "what" — the diff already shows what changed. -->

## Checklist

- [ ] `flutter analyze` is clean
- [ ] `flutter test` passes, including new tests for anything this PR adds/changes
- [ ] This plugin depends only on `omnis_plugin_api`, never on Omnis
      internals directly (see [CONTRIBUTING.md](../CONTRIBUTING.md))
- [ ] The README's plugin table is updated (new plugin, changed
      permissions/capabilities, or changed verification status) —
      including an honest verification note if this hasn't been
      exercised against real hardware/a real account
- [ ] Any network or hardware call fails soft — never throws out
      uncaught

## Testing

<!-- How did you verify this? Real device/account, or mocked — say which. -->
